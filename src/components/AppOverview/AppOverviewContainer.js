import { Component } from 'react';
import AppOverview from './AppOverview';

class AppOverviewContainer extends Component {
    render() { return AppOverview(this.props); }
}

export default AppOverviewContainer;
